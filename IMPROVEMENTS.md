# Fahem — النسخة المحسّنة

تم تحسين النسخة بناءً على فحص تجربة المستخدم للموقع.

## أهم الإصلاحات

- إضافة تأكيد عند الانتقال من سؤال دون إجابة، مع عداد للأسئلة المتروكة.
- إضافة شريط تقدم ومؤشر للصحيح/المتروك أثناء الاختبار.
- حفظ اختبار مؤقتًا في `localStorage` وإتاحة استئنافه بعد العودة.
- إضافة تحذير بصري عند اقتراب انتهاء المؤقت.
- تطوير صفحة النتيجة لتعرض الصحيح والخاطئ والمتروك وإجابات الطالب والإجابة الصحيحة.
- إضافة `aria-live` و`aria-label` و`fieldset/legend` للعناصر التعليمية والاختيارات.
- تحسين حالات التركيز بلوحة المفاتيح.
- إضافة شريط مميزات مختصر في الصفحة الرئيسية وتقليل الفراغ غير الوظيفي.
- إظهار عدد الأسئلة المتاحة في شاشة الإعداد.
- تحسين التجاوب في شريط أزرار الاختبار والنتيجة على الهاتف.

## التشغيل والتحقق

```bash
npm install
npm test
npm run check
```

للمعاينة السريعة للملفات الثابتة:

```bash
python3 -m http.server 4173
```

> تشغيل واجهات API يتطلب بيئة Cloudflare Pages Functions كما في المستودع الأصلي.

## الإصلاحات الإضافية في هذه النسخة

- ترتيب الأسئلة عشوائيًا عند بدء كل محاولة.
- ترتيب الاختيارات عشوائيًا مرة واحدة لكل محاولة مع ثبات ترتيبها عند الرجوع للسؤال.
- منع التناقض بين الدرجة وعدد المجاب؛ أصبحت صفحة النتيجة تحسب محليًا من نفس الأسئلة الظاهرة.
- عرض الصحيح والمجاب وغير المجاب والإجمالي بشكل منفصل وواضح.
- إضافة تصحيح فوري بعد اختيار الإجابة، مع الإجابة الصحيحة والتفسير.
- إعادة ترتيب الوحدات والدروس في API الضيف مثل مسار المستخدم المسجل بدل ترتيب المعرّف فقط.
- إضافة زر تسجيل الدخول في الرأس.
- إضافة مسار `/admin` ولوحة إدارة للمدير والمعلم.
- إضافة إضافة سؤال، تعديل سؤال، حذف سؤال، ورفع ملف Excel عبر لوحة الإدارة.
- ربط لوحة الإدارة بواجهات API الموجودة للصلاحيات والاستيراد والتعديل.

### ملاحظة صلاحيات الإدارة

لن يتحول الحساب العادي إلى مدير تلقائيًا. يجب أن يكون المستخدم موجودًا في قاعدة البيانات بدور `admin` أو `teacher`. عند تسجيل الدخول بهذا الدور تظهر لوحة الإدارة تلقائيًا، كما يمكن فتح `/admin` مباشرة.
