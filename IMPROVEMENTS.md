# Fahem — النسخة المحسّنة

تم تحسين النسخة بناءً على فحص تجربة المستخدم للموقع.

## أهم الإصلاحات

- إضافة تأكيد عند الانتقال من سؤال دون إجابة، مع عداد للأسئلة المتروكة.
- إضافة شريط تقدم ومؤشر للصحيح/المتروك أثناء الاختبار.
- حفظ اختبار مؤقتًا في `localStorage` وإتاحة استئنافه بعد العودة.
- إضافة تحذير بصري عند اقتراب انتهاء المؤقت.
- تطوير صفحة النتيجة لتعرض الصحيح والخاطئ والمتروك وإجابات الطالب والإجابة الصحيحة.
- إضافة `aria-live` و`aria-label` و`fieldset/legend` للعناصر التعليمية والاختيارات.
- تحسين حالات التركيز بلوحة المفاتيح.
- إضافة شريط مميزات مختصر في الصفحة الرئيسية وتقليل الفراغ غير الوظيفي.
- إظهار عدد الأسئلة المتاحة في شاشة الإعداد.
- تحسين التجاوب في شريط أزرار الاختبار والنتيجة على الهاتف.

## التشغيل والتحقق

```bash
npm install
npm test
npm run check
```

للمعاينة السريعة للملفات الثابتة:

```bash
python3 -m http.server 4173
```

> تشغيل واجهات API يتطلب بيئة Cloudflare Pages Functions كما في المستودع الأصلي.
