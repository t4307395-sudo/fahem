{
  "name": "fahem-pages",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "check": "node --check app.js && git diff --check"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
