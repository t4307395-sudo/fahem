# تدقيق قاعدة mu50 — قراءة فقط

**التاريخ:** 3 سبتمبر 2026

تم تنفيذ الاستعلامات عبر موصل Cloudflare D1 على قاعدة `mu50` بالمعرّف المرتبط بتطبيق Fahem. جميع العمليات كانت قراءة فقط؛ لم يتم تنفيذ `INSERT` أو `UPDATE` أو `DELETE`.

## ملخص الحالة

| البند | النتيجة |
|---|---:|
| المستخدمون | 8 |
| حسابات الإدارة | 1 |
| حسابات الطلاب | 7 |
| الأسئلة | 83 |
| المحاولات المكتملة | 16 |
| الرسائل | 1 |
| الأسئلة المقالية | 0 |
| الأسئلة المكررة حسب المادة/الصف/الدرس/النص | 0 |
| أسئلة بلا مادة | 0 |
| أسئلة بلا درس | 0 |
| أسئلة بلا نص | 0 |
| أسئلة بلا شرح | 0 |
| أسئلة اختيارية بلا إجابة صحيحة | 0 |
| أسئلة بخيارات غير صالحة/فارغة | 0 |

## مخطط قاعدة الإنتاج الفعلي

قاعدة الإنتاج تحتوي حاليًا على الجداول التشغيلية الأربعة المتوافقة مع `schema.sql`:

- `users`
- `questions`
- `attempts`
- `messages`

كما تظهر جداول Cloudflare الداخلية مثل `_cf_KV` و`sqlite_sequence`. لم تظهر جداول migrations القديمة مثل `students` أو `student_favorites` أو `login_rate_limits` في القاعدة الحالية.

## محتوى المنهج الحالي

كل الأسئلة الـ83 تخص:

- **المادة:** تاريخ.
- **الصف:** الصف الثاني الثانوي.
- **الوحدات/الدروس:** أربعة دروس.
- **النوع:** 82 سؤال اختيار من متعدد وسؤال واحد صح/خطأ.
- **الصعوبة:** 41 سهل و42 متوسط.
- **الصعوبة الصعبة:** لا توجد حاليًا.

التوزيع حسب الدروس:

| الدرس | سهل | متوسط | الإجمالي |
|---|---:|---:|---:|
| الدرس الأول | 18 | 7 | 25 |
| الدرس الثاني | 7 | 10 | 17 |
| الدرس الثالث | 8 | 5 | 13 |
| الدرس الرابع | 7 | 20 | 27 |
| **الإجمالي** | **40** | **42** | **82** |

يوجد سؤال صح/خطأ إضافي في الدرس الأول، وبذلك يصبح الإجمالي 83.

## المستخدمون والبيانات التعليمية

يوجد حساب إدارة واحد وسبعة طلاب. حقل `school_year` للمستخدمين غير معبأ حاليًا، وهذا يعني أن ربط الطالب بالصف الدراسي لم يُحسم في بيانات الحساب بعد. يوجد ثلاثة طلاب بلا كلمة مرور مخزنة؛ يجب عدم تعديلهم عشوائيًا قبل تحديد تدفق تسجيل الطالب المطلوب.

المحاولات المسجلة كلها مكتملة: 5 امتحانات مفتوحة و11 جلسة تدريب، ولا توجد محاولات غير مكتملة.

## الملاحظات العملية

### ما يمكن فعله الآن بأمان

يمكن البدء في بناء واجهة فهرس المنهج على مستوى المادة والدرس، لأن بيانات الأسئلة الحالية تملك مادة ودرسًا مكتملين ولا توجد تكرارات حسب معيار الفحص.

### ما ينبغي إنجازه قبل إدخال المنهج الكامل

يُنصح بإضافة أو اعتماد حقول واضحة للصف والوحدة/الفصل، ثم إدخال المنهج على دفعات مع تقرير تحقق لكل دفعة. يجب أن يرفض الاستيراد السؤال إذا كان بلا مادة أو درس أو إجابة صحيحة أو شرح، وأن يعرض عدد الأسئلة الجديدة والمكررة والمرفوضة.

كما يجب تحديد ما إذا كانت الحسابات الطلابية ستستخدم كلمة مرور فعلية لجميع الطلاب، أو تسجيلًا مبسطًا بالبريد/الاسم. وجود ثلاثة طلاب بلا كلمة مرور يدل على أن البيانات الحالية جاءت من مرحلة انتقالية.

### أولوية التطوير المقترحة

1. إضافة فهرس المنهج: الصف ← المادة ← الوحدة ← الدرس.
2. إضافة حقل الوحدة/الفصل إلى الأسئلة قبل إدخال المنهج الكامل.
3. استكمال صفحة النتيجة ومراجعة الخطأ.
4. بناء لوحة أداء الطالب.
5. إدخال المنهج على دفعات مع تحقق تلقائي.
6. إضافة الأسئلة الصعبة والمقالية بعد استقرار التصنيف.

## القرار

قاعدة `mu50` متصلة وتعمل، والبيانات الحالية نظيفة من ناحية الحقول الأساسية والتكرار. لكنها ليست «المنهج كله» بعد؛ هي بنك تاريخ للصف الثاني الثانوي مكوّن من 83 سؤالًا موزعة على أربعة دروس. يمكن التوسعة، لكن الأفضل تجهيز هيكل التصنيف والاستيراد أولًا حتى لا تتكرر مشكلة المواد أو الدروس غير المصنفة.
