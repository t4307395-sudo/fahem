# تقرير فحص مستودع Fahem

**تاريخ الفحص:** 3 سبتمبر 2026

## النطاق

تم استنساخ المستودع `t4307395-sudo/fahem` من GitHub، وفحص شجرة الملفات، وملفات الواجهة، وCloudflare Pages Functions، ومخطط D1، وسجل Git، ثم التحقق من الموقع المنشور على `https://fahem.pages.dev`. لم يتم تنفيذ أي تعديل على الكود أو قاعدة البيانات.

## الخلاصة التنفيذية

Fahem تطبيق مراجعة واختبارات عربية باتجاه RTL، مبني كتطبيق SPA ثابت على Cloudflare Pages، مع وظائف خادمية عبر Pages Functions وقاعدة Cloudflare D1 مربوطة باسم `DB`. الواجهة لا تحتاج عملية build؛ ملف `index.html` يحمل `app.js` و`styles.css` مباشرة، بينما توفر وظائف `/functions/api` المصادقة، الأسئلة، المحاولات، المفضلات، الأخطاء، الرسائل، الإحصائيات، وإدارة المعلم.

آخر commit في GitHub هو `c8bfa2a` بعنوان **Clarify duplicate Excel import result**، والفرع `main` نظيف محليًا ومتطابق مع `origin/main`. الموقع المنشور يستجيب بنجاح، ويقدم 83 سؤالًا عبر المسار العام `/api/questions`، ولا يعرض `correct_answer` في الاستجابة العامة.

## البنية البرمجية

| الجزء | الموقع | الوظيفة |
|---|---|---|
| نقطة التطبيق | `index.html` | مستند عربي RTL وحاوية SPA وتحميل الملفات الثابتة |
| منطق الواجهة | `app.js` | التنقل، تسجيل الدخول، أوضاع التدريب والاختبار والمراجعة، لوحة المعلم، PWA |
| التصميم | `styles.css` | الهوية البصرية والتخطيط المتجاوب |
| وظائف Pages | `functions/api/` | API الخادمي المتصل بـ D1 |
| طبقة مشتركة | `functions/api/_shared.js` | JSON، قراءة body، PBKDF2، الجلسات، الصلاحيات، حراسة Origin |
| حماية الاستجابات | `functions/_middleware.js` و`_headers` | CSP وheaders أمنية ومعالجة الاستجابات |
| قاعدة البيانات | `schema.sql` و`migrations/` | مخطط الجداول والترقيات المرحلية |
| استيراد Excel | `functions/api/questions/xlsx-bundle.js` | حزمة parser مضمّنة لتفادي الاعتماد على build خارجي |
| الاختبارات | `tests/ui-contract.test.js` | عقود أساسية لتدفقات الطالب والواجهة |

## مسارات API

- `/api/auth`: تسجيل/إنشاء طالب وتسجيل الدخول، مع جلسة Cookie باسم `mr_session`.
- `/api/auth/me` و`/api/auth/logout`: قراءة الجلسة وإنهاؤها.
- `/api/questions`: قراءة بنك الأسئلة للمستخدمين، وإضافة سؤال للمعلم.
- `/api/questions/:id`: تعديل أو حذف سؤال للمعلم.
- `/api/questions/import`: استيراد أسئلة Excel/CSV مع إزالة التكرارات.
- `/api/answer`: التحقق من الإجابة وتسجيل نتيجة المحاولة.
- `/api/attempts`: إنشاء وتسليم محاولات التدريب/الاختبار/المراجعة.
- `/api/favorites`: قراءة وإضافة وحذف مفضلات الطالب.
- `/api/mistakes`: استخراج الأسئلة الخاطئة للطالب.
- `/api/stats`: إحصاءات الطالب حسب المادة واليوم.
- `/api/essays`: قائمة وتصحيح الإجابات المقالية للمعلم.
- `/api/contact`: رسائل الدعم وبلاغات الأسئلة.
- `/api/settings/exam`: قراءة وضبط مدة الامتحان للمعلم.

## مخطط D1

المخطط الأساسي في `schema.sql` يعرّف أربع جداول تشغيلية:

1. `users`: المستخدمون والأدوار `student` و`teacher` و`admin`، مع إعدادات وإحصاءات ومفضلات JSON.
2. `questions`: المادة، الدرس، الصف، النوع، السؤال، الخيارات، الإجابة الصحيحة، الشرح، والصعوبة.
3. `attempts`: محاولات الطالب وإجاباته ونتائجه وتواريخ البدء والإكمال.
4. `messages`: رسائل الدعم وبلاغات الأسئلة وطلبات استعادة كلمة المرور.

وتوجد migrations تاريخية تضيف/تعدّل جداول مثل `student_favorites` و`contact_messages` و`login_rate_limits`، لكن بعضها يستند إلى مخطط أقدم (`students` بدل `users`). لذلك لا يمكن اعتبارها مطبقة على قاعدة الإنتاج الحالية دون فحص D1 نفسه.

ملف `wrangler.toml` يربط التطبيق بقاعدة D1 اسمها `mu50` ومعرّفها محفوظ في إعدادات النشر، والـ binding المتوقع داخل الوظائف هو `env.DB`. سر `SESSION_SECRET` مطلوب في بيئة Cloudflare ولا ينبغي أن يكون داخل Git.

## التحقق المنفّذ

- `pnpm install --frozen-lockfile`: نجح.
- `pnpm test`: نجح؛ **9 اختبارات** في ملف واحد.
- `pnpm check`: نجح؛ فحص صياغة `app.js` و`git diff --check`.
- `https://fahem.pages.dev/`: HTTP 200.
- `https://fahem.pages.dev/api/auth`: HTTP 200 ويرجع مستخدمًا غير مسجل.
- `https://fahem.pages.dev/api/auth/me`: HTTP 401 بدون جلسة، وهو متوقع.
- `https://fahem.pages.dev/api/questions`: HTTP 200 ويرجع 83 سؤالًا.
- `/manifest.json` و`/sw.js`: كلاهما HTTP 200، ما يؤكد وجود أساس PWA.
- الرؤوس المنشورة تتضمن CSP، HSTS، `X-Content-Type-Options`، `Referrer-Policy` و`Permissions-Policy`.

## ملاحظات ومخاطر تستحق المتابعة

### 1. فحص بيانات D1 الفعلية لم يُنفذ
تم فحص المخطط والكود والاستجابة العامة فقط. لا يوجد موصل Cloudflare/D1 مفعّل في جلسة Manus الحالية، لذلك لم أقرأ صفوف `users` أو `questions` أو `attempts` من قاعدة الإنتاج، ولم أنفذ أي SQL. هذا يمنع الجزم بنظافة البيانات، التكرارات، الحسابات الإدارية، أو تطبيق جميع migrations على الإنتاج.

### 2. حماية تسجيل الدخول
الدالة `loginRateAllowed` في `_shared.js` ترجع `true` حاليًا، رغم وجود migration باسم `phase6_login_rate_limits.sql`. ينبغي مطابقة الكود الحالي مع migration والتأكد من أن تحديد المعدل مطبق فعليًا في الإنتاج.

### 3. سياسة CORS
الاستجابات المنشورة تظهر `access-control-allow-origin: *`. هذا ليس تسريبًا مباشرًا للجلسة بسبب Cookie `HttpOnly` و`SameSite=Lax`، لكنه أوسع من اللازم لتطبيق يعتمد على جلسات؛ يُفضّل تقييده إلى الأصل الرسمي إذا لم توجد حاجة لاستهلاك API من أصول أخرى.

### 4. نموذج الجلسة
الجلسة موقعة بـ SHA-256 على payload مع `SESSION_SECRET` وتحتوي الدور والبريد والمدة. هذا مقبول كرمز موقّع بسيط، لكن إبطال الجلسات الفردية أو تدويرها غير متاح من المخطط الحالي؛ تغيير سر الجلسات هو آلية الإبطال الشاملة.

### 5. الحالة المنشورة
الواجهة المنشورة تحمل cache-buster باسم `fahem-upgrade-20260902`، بينما commit GitHub الحالي هو `c8bfa2a`. لا يمكن إثبات علاقة مباشرة بين اسم cache-buster وcommit hash، لكن الملفات الأساسية والمسارات العامة تعمل؛ يُفضّل توثيق deployment ID في Cloudflare Pages عند الحاجة إلى مطابقة قطعية.

## ما لم أفعله عمدًا

لم أعدّل الملفات، ولم أرفع commit جديدًا، ولم أحذف أو أنظف بيانات، ولم أستخدم أي SQL على D1، ولم أحاول استنتاج أو استخراج أسرار Cloudflare. كذلك لم أفتح صلاحية جديدة لأن موصل Cloudflare غير موجود في الإعدادات الحالية.

## الخطوة التالية المقترحة

إذا كان المطلوب الآن **تدقيق قاعدة الإنتاج نفسها**، فالخطوة اللازمة هي تمكين/ربط موصل Cloudflare أو تزويد الجلسة بموصل D1 مصرح. بعد ذلك يمكن إجراء قراءة غير مدمرة فقط للتحقق من: عدد الصفوف، تطبيق المخطط والترقيات، التكرارات في الأسئلة، الحسابات والأدوار، المحاولات غير المكتملة، الرسائل، واتساق JSON. أما لفهم البنية البرمجية والنشر العام، فقد اكتمل الفحص دون الحاجة إلى ذلك الوصول.
